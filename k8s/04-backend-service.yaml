apiVersion: v1
kind: Service
metadata:
  name: bankchase-backend
  namespace: bankchase
  labels:
    app: bankchase-backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: bankchase-backend
